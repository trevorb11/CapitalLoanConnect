<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>You're Approved! | Today Capital Group</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-bg-dark: #0B1120;
            --color-bg-card: #111827;
            --color-teal: #14B8A6;
            --color-teal-light: #2DD4BF;
            --color-teal-dark: #0D9488;
            --color-teal-glow: rgba(20, 184, 166, 0.15);
            --color-teal-glow-strong: rgba(20, 184, 166, 0.25);
            --color-white: #FFFFFF;
            --color-gray-400: #9CA3AF;
            --color-gray-500: #6B7280;
            --color-gray-700: #374151;
            --color-border: rgba(255, 255, 255, 0.08);
            --font-main: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            --radius: 12px;
            --radius-lg: 16px;
            --radius-full: 9999px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-main);
            background: var(--color-bg-dark);
            color: var(--color-white);
            line-height: 1.6;
            min-height: 100vh;
        }

        .bg-gradient {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(ellipse 80% 50% at 50% -20%, var(--color-teal-glow) 0%, transparent 50%),
                radial-gradient(ellipse 60% 40% at 100% 100%, rgba(20, 184, 166, 0.08) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        .header {
            position: relative;
            z-index: 10;
            padding: 20px 24px;
            border-bottom: 1px solid var(--color-border);
        }

        .header-content {
            max-width: 1200px;
            margin: 0 auto;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            width: 40px;
            height: 40px;
            background: var(--color-teal);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo-icon svg {
            width: 24px;
            height: 24px;
            color: var(--color-bg-dark);
        }

        .logo-text {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--color-white);
        }

        .logo-text span {
            color: var(--color-teal);
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 48px 24px;
            position: relative;
            z-index: 1;
        }

        /* Hero */
        .hero {
            text-align: center;
            margin-bottom: 48px;
            animation: fadeInUp 0.6s ease-out;
        }

        .badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: var(--color-teal-glow-strong);
            border: 1px solid rgba(20, 184, 166, 0.3);
            color: var(--color-teal-light);
            padding: 10px 20px;
            border-radius: var(--radius-full);
            font-size: 0.875rem;
            font-weight: 600;
            margin-bottom: 32px;
        }

        .badge svg {
            width: 18px;
            height: 18px;
        }

        .greeting {
            font-size: 1.125rem;
            color: var(--color-gray-400);
            margin-bottom: 8px;
        }

        .merchant-name {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--color-white);
            margin-bottom: 32px;
        }

        .approved-headline {
            font-size: 2rem;
            font-weight: 800;
            line-height: 1.2;
            margin-bottom: 16px;
        }

        .amount-display {
            margin: 32px 0;
        }

        .amount {
            font-size: 4.5rem;
            font-weight: 800;
            color: var(--color-teal);
            letter-spacing: -0.03em;
            line-height: 1;
            text-shadow: 0 0 60px var(--color-teal-glow-strong);
        }

        .net-amount {
            margin-top: 12px;
            font-size: 1.125rem;
            color: var(--color-gray-400);
        }

        .net-amount strong {
            color: var(--color-white);
            font-weight: 600;
        }

        /* Offer Section - Clean horizontal layout */
        .offer-section {
            background: var(--color-bg-card);
            border: 1px solid var(--color-border);
            border-radius: var(--radius-lg);
            padding: 32px;
            margin-bottom: 24px;
            animation: fadeInUp 0.6s ease-out 0.1s both;
        }

        .offer-label {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 32px;
        }

        .offer-title {
            font-size: 0.8125rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--color-gray-500);
        }

        .offer-program {
            font-size: 0.8125rem;
            font-weight: 600;
            color: var(--color-teal);
        }

        /* Weekly payment as the star */
        .weekly-hero {
            text-align: center;
            padding-bottom: 32px;
            border-bottom: 1px solid var(--color-border);
            margin-bottom: 28px;
        }

        .weekly-amount {
            font-size: 3.5rem;
            font-weight: 800;
            color: var(--color-white);
            letter-spacing: -0.02em;
            line-height: 1;
        }

        .weekly-label {
            margin-top: 8px;
            font-size: 0.9375rem;
            color: var(--color-teal);
            font-weight: 600;
        }

        /* Term details as supporting info */
        .term-details {
            display: flex;
            justify-content: center;
            gap: 48px;
        }

        .term-item {
            text-align: center;
        }

        .term-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--color-white);
            margin-bottom: 4px;
        }

        .term-label {
            font-size: 0.75rem;
            color: var(--color-gray-500);
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        /* Fee Breakdown */
        .card {
            background: var(--color-bg-card);
            border: 1px solid var(--color-border);
            border-radius: var(--radius-lg);
            padding: 28px;
            margin-bottom: 24px;
            animation: fadeInUp 0.6s ease-out 0.2s both;
        }

        .card-header {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 20px;
        }

        .card-icon {
            width: 36px;
            height: 36px;
            background: var(--color-teal-glow-strong);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .card-icon svg {
            width: 18px;
            height: 18px;
            color: var(--color-teal);
        }

        .card-title {
            font-size: 0.9375rem;
            font-weight: 700;
            color: var(--color-white);
        }

        .fee-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid var(--color-border);
        }

        .fee-row:last-child {
            border-bottom: none;
            padding-top: 16px;
            margin-top: 4px;
            border-top: 2px solid var(--color-gray-700);
        }

        .fee-label {
            color: var(--color-gray-400);
            font-size: 0.875rem;
        }

        .fee-value {
            font-weight: 600;
            font-size: 0.875rem;
            color: var(--color-white);
        }

        .fee-value.positive {
            color: var(--color-teal);
        }

        .fee-value.negative {
            color: var(--color-gray-500);
        }

        .fee-row:last-child .fee-label,
        .fee-row:last-child .fee-value {
            font-size: 0.9375rem;
            font-weight: 700;
        }

        /* Features */
        .features-row {
            display: flex;
            justify-content: center;
            gap: 28px;
            flex-wrap: wrap;
            margin-bottom: 32px;
            animation: fadeInUp 0.6s ease-out 0.3s both;
        }

        .feature-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: var(--color-gray-400);
            font-size: 0.875rem;
        }

        .feature-item svg {
            width: 18px;
            height: 18px;
            color: var(--color-teal);
        }

        /* CTA */
        .cta-section {
            background: linear-gradient(135deg, var(--color-teal-dark) 0%, var(--color-teal) 100%);
            border-radius: var(--radius-lg);
            padding: 36px;
            text-align: center;
            position: relative;
            overflow: hidden;
            animation: fadeInUp 0.6s ease-out 0.4s both;
        }

        .cta-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
            opacity: 0.5;
        }

        .cta-content {
            position: relative;
            z-index: 1;
        }

        .cta-section h3 {
            font-size: 1.375rem;
            font-weight: 800;
            margin-bottom: 6px;
            color: var(--color-bg-dark);
        }

        .cta-section p {
            color: rgba(11, 17, 32, 0.7);
            margin-bottom: 24px;
            font-size: 0.9375rem;
        }

        .cta-buttons {
            display: flex;
            gap: 12px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 14px 28px;
            border-radius: var(--radius-full);
            font-weight: 700;
            font-size: 0.9375rem;
            text-decoration: none;
            transition: all 0.3s ease;
            cursor: pointer;
            border: none;
        }

        .btn-primary {
            background: var(--color-bg-dark);
            color: var(--color-white);
        }

        .btn-primary:hover {
            background: #1a2332;
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
        }

        .btn-secondary {
            background: rgba(11, 17, 32, 0.15);
            color: var(--color-bg-dark);
            border: 2px solid rgba(11, 17, 32, 0.2);
        }

        .btn-secondary:hover {
            background: rgba(11, 17, 32, 0.25);
            transform: translateY(-2px);
        }

        .btn svg {
            width: 18px;
            height: 18px;
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 28px 24px;
            color: var(--color-gray-500);
            font-size: 0.8125rem;
            animation: fadeInUp 0.6s ease-out 0.5s both;
        }

        .footer p {
            margin-bottom: 4px;
        }

        .rep-info {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            margin-top: 14px;
            padding-top: 14px;
            border-top: 1px solid var(--color-border);
            flex-wrap: wrap;
        }

        .rep-info span {
            display: flex;
            align-items: center;
            gap: 6px;
            color: var(--color-gray-400);
            font-size: 0.8125rem;
        }

        .rep-info svg {
            width: 14px;
            height: 14px;
            color: var(--color-teal);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(24px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (max-width: 640px) {
            .container {
                padding: 32px 16px;
            }

            .approved-headline {
                font-size: 1.5rem;
            }

            .amount {
                font-size: 3rem;
            }

            .weekly-amount {
                font-size: 2.5rem;
            }

            .term-details {
                gap: 32px;
            }

            .cta-section {
                padding: 28px 20px;
            }

            .features-row {
                gap: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="bg-gradient"></div>

    <header class="header">
        <div class="header-content">
            <div class="logo">
                <div class="logo-icon">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/>
                        <polyline points="17 6 23 6 23 12"/>
                    </svg>
                </div>
                <div class="logo-text">Today Capital <span>Group</span></div>
            </div>
        </div>
    </header>

    <div class="container">
        <!-- Hero -->
        <section class="hero">
            <div class="badge">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                    <polyline points="22 4 12 14.01 9 11.01"/>
                </svg>
                Approved
            </div>

            <p class="greeting">Congratulations,</p>
            <!-- MERGE: {{contact.company_name}} -->
            <h1 class="merchant-name">ABC Trucking LLC</h1>

            <h2 class="approved-headline">
                You've Been Approved For Up To
            </h2>

            <div class="amount-display">
                <!-- MERGE: {{approval.advance_amount}} -->
                <div class="amount">$95,500</div>
                <!-- MERGE: {{approval.net_amount}} -->
                <p class="net-amount"><strong>$92,136</strong> net after fees</p>
            </div>
        </section>

        <!-- Offer Section -->
        <section class="offer-section">
            <div class="offer-label">
                <span class="offer-title">Your Offer</span>
                <!-- MERGE: {{approval.program}} -->
                <span class="offer-program">Platinum Program</span>
            </div>

            <div class="weekly-hero">
                <!-- MERGE: {{approval.weekly_payment}} -->
                <div class="weekly-amount">$1,752</div>
                <div class="weekly-label">per week</div>
            </div>

            <div class="term-details">
                <div class="term-item">
                    <!-- MERGE: {{approval.term}} -->
                    <div class="term-value">18 mo</div>
                    <div class="term-label">Term</div>
                </div>
                <div class="term-item">
                    <!-- MERGE: {{approval.factor_rate}} -->
                    <div class="term-value">1.42</div>
                    <div class="term-label">Factor Rate</div>
                </div>
                <div class="term-item">
                    <!-- MERGE: {{approval.total_payback}} -->
                    <div class="term-value">$135,610</div>
                    <div class="term-label">Total Payback</div>
                </div>
            </div>
        </section>

        <!-- Fee Breakdown -->
        <div class="card">
            <div class="card-header">
                <div class="card-icon">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <rect x="2" y="3" width="20" height="14" rx="2" ry="2"/>
                        <line x1="2" y1="10" x2="22" y2="10"/>
                    </svg>
                </div>
                <h3 class="card-title">Funding Breakdown</h3>
            </div>
            <div class="fee-row">
                <span class="fee-label">Advance Amount</span>
                <!-- MERGE: {{approval.advance_amount}} -->
                <span class="fee-value positive">+ $95,500.00</span>
            </div>
            <div class="fee-row">
                <span class="fee-label">Origination Fee (3%)</span>
                <!-- MERGE: {{approval.origination_fee}} -->
                <span class="fee-value negative">- $2,865.00</span>
            </div>
            <div class="fee-row">
                <span class="fee-label">Application Fee</span>
                <!-- MERGE: {{approval.application_fee}} -->
                <span class="fee-value negative">- $499.00</span>
            </div>
            <div class="fee-row">
                <span class="fee-label">You Receive</span>
                <!-- MERGE: {{approval.net_amount}} -->
                <span class="fee-value">$92,136.00</span>
            </div>
        </div>

        <!-- Features -->
        <div class="features-row">
            <div class="feature-item">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/>
                    <polyline points="17 6 23 6 23 12"/>
                </svg>
                Pre-Pay Option
            </div>
            <div class="feature-item">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                    <line x1="16" y1="2" x2="16" y2="6"/>
                    <line x1="8" y1="2" x2="8" y2="6"/>
                    <line x1="3" y1="10" x2="21" y2="10"/>
                </svg>
                Weekly Payments
            </div>
            <div class="feature-item">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="12" cy="12" r="10"/>
                    <polyline points="12 6 12 12 16 14"/>
                </svg>
                24-48hr Funding
            </div>
        </div>

        <!-- CTA -->
        <section class="cta-section">
            <div class="cta-content">
                <h3>Ready to Move Forward?</h3>
                <p>No obligation — let's get you funded.</p>
                <div class="cta-buttons">
                    <!-- MERGE: {{rep.calendar_link}} -->
                    <a href="https://calendly.com/tcg-funding" class="btn btn-primary">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
                            <line x1="16" y1="2" x2="16" y2="6"/>
                            <line x1="8" y1="2" x2="8" y2="6"/>
                            <line x1="3" y1="10" x2="21" y2="10"/>
                        </svg>
                        Schedule a Call
                    </a>
                    <!-- MERGE: {{rep.phone}} -->
                    <a href="tel:+18005551234" class="btn btn-secondary">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                        </svg>
                        Call Now
                    </a>
                </div>
            </div>
        </section>

        <!-- Footer -->
        <footer class="footer">
            <p>This approval is valid for 30 days and subject to final verification.</p>
            <div class="rep-info">
                <span>
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                        <circle cx="12" cy="7" r="4"/>
                    </svg>
                    <!-- MERGE: {{rep.name}} -->
                    John Smith
                </span>
                <span>
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                        <polyline points="22,6 12,13 2,6"/>
                    </svg>
                    <!-- MERGE: {{rep.email}} -->
                    john@todaycapitalgroup.com
                </span>
            </div>
        </footer>
    </div>
</body>
</html>